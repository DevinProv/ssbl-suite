Players
    PlayerID (PK)
    PlayerName
    DefaultChar (string, nullable)
    DefaultCharColor (string, nullable)

Events
    EventID (PK)
    EventTItle
    BracketLink

Sets
    SetID (pk)
    EventID (FK -> Events)
    Round
    Player1ID (FK -> Players)
    Player2ID (FK -> Players)
    WinnerID (FK -> Players)
    VODFileName
    VDOTimestampStart
    VODTimeStampEnd

Games
    GameID (PK)
    SetID (FK -> Sets)
    GameNumber
    P1Char
    P2Char
    WinnerID (FK -> Players)



